@page
@model Lab.Pages.Login.LandingPageModel
@{
}
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Arvo&display=swap" rel="stylesheet">


<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Archivo:wght@500&family=Arvo&display=swap" rel="stylesheet">

@{
    // if username is not null, then display login message
    if (HttpContext.Session.GetString("username") != null)
    {
        <p>User @HttpContext.Session.GetString("username") is logged in!</p>
        <a class="btn btn-purple" asp-page="HashedLogin" asp-route-logout="true">Logout</a>
    }


    //if (logout != null)
    //{
    //    <p class="text-success">@ViewData["ErrorMessage"]</p>
    //}
    //else
    //{
    //    <p class="text-danger">@ViewData["LoginMessage"]</p>
    //}

}
