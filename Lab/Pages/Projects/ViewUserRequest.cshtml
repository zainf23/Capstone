@page
@model Lab.Pages.Projects.ViewUserRequestModel
@{
}
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Arvo&display=swap" rel="stylesheet">


<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Archivo:wght@100&family=Arvo&display=swap" rel="stylesheet">

<h2 class="h2">Requests To Join Project Team:</h2>
<table class="table table-bordered table-resposnive table triped table-hover">
    <thead class="table-dark">
        <tr>
            <th class="">Name:</th>
            <th class="">Email:</th>
            <th class="">User Type:</th>
            <th class="">Pitch:</th>
            <th class="">Accept:</th>
            <th class="">Reject:</th>


        </tr>
    </thead>
    <tbody>
                <tr>

                    <td>@Model.MyRequests.firstName @Model.MyRequests.secondName</td>
                    <td>@Model.MyRequests.email</td>
                    <td>@Model.MyRequests.jmuType</td>
                    <td>@Model.MyRequests.userPitch</td>
                    <td>
                        <a asp-page="Accept" class="btn btn-primary" asp-route-requestid="@Model.MyRequests.requestID">Accept</a>
                    </td>
                    <td>
                        <a asp-page="Reject" class="btn btn-primary" asp-route-requestid="@Model.MyRequests.requestID">Reject</a>
                    </td>

                </tr>
    </tbody>
</table>

